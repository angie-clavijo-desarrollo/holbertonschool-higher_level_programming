#!/usr/bin/python3
def replace_in_list(my_list, idx, element):

    if idx < (my_list[idx]):
        if idx == idx:
            my_list[idx] = element
            print(my_list)
    if idx > (my_list[idx]):
        if idx == idx:
            my_list[idx] = element
            print(my_list)
