#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    if tuple_a[0] != tuple_a[1]:
        return (tuple_a[1], tuple_a[1]+tuple_b[1])
    if tuple_a[0] !=  (tuple_a[0] + tuple_a[0]):
        return ((tuple_a[0]+tuple_a[0], tuple_a[1]))
    if tuple_a == tuple_a:
        return (tuple_a)
